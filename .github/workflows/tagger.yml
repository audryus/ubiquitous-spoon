name: Bump version
on:
  push:
    branches:
      - trunk
jobs:
  tag:
    runs-on: ubuntu-22.04
    steps:
    - uses: actions/checkout@v3
      with:
        fetch-depth: '0'
    - name: Push tag
      run: |
          git tag ${{github.run_id}}
          git push origin ${{github.run_id}}
